import React from 'react';
import styled from 'styled-components'

const HeroContact = () => {
  return <HeroContainer>HeroContactaa</HeroContainer>;
};

export default HeroContact;

const HeroContainer = styled.section`
  height: 100vh;
  justify-content: space-evenly;
  display: flex;
  align-items: center;
`;
